{
  "name": "@rowsncolumns/export",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc -b",
    "build:watch": "tsc -p . --watch",
    "check:types": "tsc --noEmit",
    "prepublish": "yarn build",
    "prettier": "prettier src/**/*.{ts,tsx} --write",
    "test": "jest"
  },
  "dependencies": {
    "@rowsncolumns/spreadsheet": "^1.4.4",
    "exceljs": "^4.1.0"
  }
}
